# RotatedPosition

## Overview
- Documentation for `RotatedPosition`.
- Declared as a class in `com.hypixel.hytale.builtin.hytalegenerator.props.directionality`.

## Constructors
- `RotatedPosition(int x, int y, int z, @Nonnull PrefabRotation rotation)`
  - Creates a `RotatedPosition` instance.
- `RotatedPosition(x, y, z, rotation)`
  - Creates a `RotatedPosition` instance.

## Methods
- `getRelativeTo(@Nonnull RotatedPosition other)`
  - Executes `getRelativeTo` behavior.
- `toVector3i()`
  - Executes `toVector3i` behavior.

## Notes
- No additional notes.
