# VoxelCoordinate

## Overview
- Documentation for `VoxelCoordinate`.
- Declared as a class in `com.hypixel.hytale.builtin.hytalegenerator.datastructures.voxelspace`.

## Constructors
- `VoxelCoordinate(int x, int y, int z)`
  - Creates a `VoxelCoordinate` instance.
- `VoxelCoordinate(this.x, this.y, this.z)`
  - Creates a `VoxelCoordinate` instance.

## Methods
- `equals(Object other)`
  - Executes `equals` behavior.
- `clone()`
  - Executes `clone` behavior.
- `toString()`
  - Executes `toString` behavior.

## Notes
- No additional notes.
