# WarpRemoveCommand

## Overview
- Documentation for `WarpRemoveCommand`.
- Declared as a class in `com.hypixel.hytale.builtin.teleport.commands.warp`.

## Constructors
- `WarpRemoveCommand()`
  - Creates a `WarpRemoveCommand` instance.

## Methods
- `executeSync(@Nonnull CommandContext context)`
  - Executes `executeSync` behavior.

## Notes
- No additional notes.
