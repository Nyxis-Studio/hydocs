# SensorLight

## Overview
- Documentation for `SensorLight`.
- Declared as a class in `com.hypixel.hytale.server.npc.corecomponents.world`.

## Constructors
- `SensorLight(@Nonnull BuilderSensorLight builderSensorLight, @Nonnull BuilderSupport builderSupport)`
  - Creates a `SensorLight` instance.

## Methods
- `matches(@Nonnull Ref<EntityStore> ref, @Nonnull Role role, double dt, @Nonnull Store<EntityStore> store)`
  - Executes `matches` behavior.
- `getSensorInfo()`
  - Executes `getSensorInfo` behavior.

## Notes
- No additional notes.
