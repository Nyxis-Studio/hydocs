# OperatorUnary

## Overview
- Documentation for `OperatorUnary`.
- Declared as a class in `com.hypixel.hytale.server.npc.util.expression.compile`.

## Constructors
- `OperatorUnary(Token token, ValueType argument, ValueType result, Function<Scope, ExecutionContext.Instruction> codeGen)`
  - Creates a `OperatorUnary` instance.
- `OperatorUnary(token, argument, result, codeGen)`
  - Creates a `OperatorUnary` instance.

## Methods
- `hasCodeGen()`
  - Executes `hasCodeGen` behavior.
- `getResultType()`
  - Executes `getResultType` behavior.
- `of(Token token, ValueType argument, ValueType result, Function<Scope, ExecutionContext.Instruction> codeGen)`
  - Executes `of` behavior.
- `findOperator(Token token, ValueType type)`
  - Executes `findOperator` behavior.

## Notes
- No additional notes.
