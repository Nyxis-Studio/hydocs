# UnbanCommand

## Overview
- Documentation for `UnbanCommand`.
- Declared as a class in `com.hypixel.hytale.server.core.modules.accesscontrol.commands`.

## Constructors
- `UnbanCommand(@Nonnull HytaleBanProvider banProvider)`
  - Creates a `UnbanCommand` instance.

## Methods
- `executeAsync(@Nonnull CommandContext context)`
  - Executes `executeAsync` behavior.

## Notes
- No additional notes.
