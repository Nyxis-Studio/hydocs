# WorldConfigSetSpawnDefaultCommand

## Overview
- Documentation for `WorldConfigSetSpawnDefaultCommand`.
- Declared as a class in `com.hypixel.hytale.server.core.universe.world.commands.worldconfig`.

## Constructors
- `WorldConfigSetSpawnDefaultCommand()`
  - Creates a `WorldConfigSetSpawnDefaultCommand` instance.

## Methods
- `execute(@Nonnull CommandContext context, @Nonnull World world, @Nonnull Store<EntityStore> store)`
  - Executes `execute` behavior.

## Notes
- No additional notes.
