# PluginCommand

## Overview
- Documentation for `PluginCommand`.
- Declared as a class in `com.hypixel.hytale.server.core.plugin.commands`.

## Constructors
- `PluginCommand()`
  - Creates a `PluginCommand` instance.

## Methods
- `parse(@Nonnull String input, @Nonnull ParseResult parseResult)`
  - Executes `parse` behavior.
- `executeSync(@Nonnull CommandContext context)`
  - Executes `executeSync` behavior.
- `execute(@Nonnull CommandContext context, @Nonnull Store<EntityStore> store, @Nonnull Ref<EntityStore> ref, @Nonnull PlayerRef playerRef, @Nonnull World world)`
  - Executes `execute` behavior.

## Notes
- No additional notes.
