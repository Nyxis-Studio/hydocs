# AssetRegistry

## Overview
- Documentation for `AssetRegistry`.
- Declared as a class in `com.hypixel.hytale.server.core.plugin.registry`.

## Constructors
- `AssetRegistry(List<BooleanConsumer> unregister)`
  - Creates a `AssetRegistry` instance.

## Methods
- `register(@Nonnull S assetStore)`
  - Executes `register` behavior.
- `shutdown()`
  - Executes `shutdown` behavior.

## Notes
- No additional notes.
